{{- if .Values.hooks.bootstrap.enabled }}
apiVersion: batch/v1
kind: Job
metadata:
  name: {{ include "identity.fullname" . }}-bootstrap
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "identity.labels" . | nindent 4 }}
    app.kubernetes.io/component: bootstrap
  annotations:
    {{- include "identity.hookAnnotations" . | nindent 4 }}
    helm.sh/hook-weight: "20"
    {{- with .Values.hooks.bootstrap.annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  activeDeadlineSeconds: {{ .Values.hooks.bootstrap.activeDeadlineSeconds }}
  backoffLimit: {{ .Values.hooks.bootstrap.backoffLimit }}
  ttlSecondsAfterFinished: {{ .Values.hooks.bootstrap.ttlSecondsAfterFinished }}
  template:
    metadata:
      labels:
        {{- include "identity.labels" . | nindent 8 }}
        app.kubernetes.io/component: bootstrap
    spec:
      restartPolicy: Never
      serviceAccountName: {{ include "identity.hookServiceAccountName" . }}
      automountServiceAccountToken: false
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: identity
          image: {{ include "identity.image" . | quote }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command: [node, dist/bootstrap-client.cjs]
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          resources:
            {{- toYaml .Values.hooks.bootstrap.resources | nindent 12 }}
          env:
            {{- include "identity.configEnv" . | nindent 12 }}
            {{- include "identity.runtimeSecretEnv" . | nindent 12 }}
          {{- with .Values.bootstrapVolumeMounts }}
          volumeMounts:
            {{- toYaml . | nindent 12 }}
          {{- end }}
      {{- with .Values.volumes }}
      volumes:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
